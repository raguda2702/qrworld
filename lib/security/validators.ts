import { z } from "zod"; export const DateProfileSchema = z.object({ name: z.string().trim().min(2).max(60), age: z.number().int().min(18).max(99), bio: z.string().trim().min(10).max(500), city: z.string().trim().min(2).max(80), interests: z.array(z.string().trim().min(1).max(40)).max(20), avatarUrl: z.string().optional(), visibility: z.enum(["public","private"]).default("public") }); export const QRCreateSchema = z.object({ service: z.enum(["date","pet","door","car","kids","stuff"]), targetId: z.string().trim().min(3).max(120), visibility: z.enum(["public","private"]).default("public") });