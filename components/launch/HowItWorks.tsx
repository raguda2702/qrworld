const steps = [["Create", "Set up your account, profile and service pages."],["Generate", "Create QR codes linked to the exact public record."],["Attach", "Place your QR on products, cards, stickers or apparel."],["Track", "Monitor scans, plans and conversion from your dashboard."]];
export function HowItWorks() { return <section className="container-page pt-8"><div className="surface p-6 sm:p-8"><h2 className="section-title">How it works</h2><div className="mt-6 grid gap-4 md:grid-cols-4">{steps.map(([title, body], idx) => <div key={title} className="rounded-3xl border border-slate-200 bg-white p-4"><div className="text-sm font-medium text-slate-400">Step {idx + 1}</div><div className="mt-2 text-lg font-semibold">{title}</div><p className="mt-2 text-sm text-slate-500">{body}</p></div>)}</div></div></section>; }
