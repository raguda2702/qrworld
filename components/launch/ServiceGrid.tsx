const items = [["QR Date", "Beautiful public dating profiles and scan analytics."],["QR Pet", "Pet passport, care info and owner contact pages."],["QR Door", "Access and visitor communication flows."],["QR Car", "Vehicle info, contact and service records."],["QR Kids", "Emergency-safe parent contact and info pages."],["QR Stuff", "Smart labels for personal belongings and products."]];
export function ServiceGrid() { return <section className="container-page pt-8"><div className="mb-4"><h2 className="section-title">Services inside one ecosystem</h2><p className="section-subtitle">Each service can share the same account, billing and QR workflow.</p></div><div className="card-grid">{items.map(([title, body]) => <div key={title} className="touch-card"><div className="text-lg font-semibold">{title}</div><p className="mt-2 text-sm text-slate-500">{body}</p></div>)}</div></section>; }
